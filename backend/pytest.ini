[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: tests unitaires sans I/O (DB/LLM/reseau)
    integration: tests d'integration (DB / API / RLS)
    eval: tests d'evaluation LLM (golden set, exclus du run par defaut)
