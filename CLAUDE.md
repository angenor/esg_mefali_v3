# ESG Mefali - Conseiller ESG IA

Plateforme conversationnelle IA qui democratise l'acces a la finance durable pour les PME africaines francophones. Combine analyse de conformite ESG, conseil en financement vert et scoring de credit alternatif.

## Stack Technologique

### Frontend (Nuxt 4)
- **Framework** : Nuxt 4 + Vue Composition API
- **State** : Pinia
- **UI** : TailwindCSS + GSAP (animations)
- **Editeur** : toast-ui/editor
- **Graphiques** : Chart.js
- **IA Client** : LangGraph + LangChain (couche utilitaire)

### Backend (FastAPI)
- **Framework** : FastAPI (Python)
- **LLM** : Claude API (Anthropic) via OpenRouter
- **BDD** : PostgreSQL + pgvector (embeddings)
- **Stockage** : Local (MinIO/S3 plus tard)
- **Queue** : Synchrone (Redis + Celery plus tard)

## Architecture Modulaire

Le projet est organise en 8 modules :

1. **Agent Conversationnel** — Chat multimodal FR, profilage entreprise, memoire contextuelle
2. **Analyseur Conformite ESG** — Upload/OCR documents, grille E-S-G contextualisee Afrique, scoring dynamique /100, rapport PDF
3. **Conseiller Financement Vert** — BDD fonds (GCF, FEM, BOAD, BAD...), matching projet-financement, generateur de dossiers
4. **Calculateur Empreinte Carbone** — Questionnaire adapte contexte africain, calcul tCO2e, plan de reduction
5. **Scoring Credit Vert Alternatif** — Donnees non-conventionnelles (Mobile Money, photos IA), score hybride solvabilite+impact
6. **Plan d'Action** — Feuille de route 6-12-24 mois, rappels cron, bibliotheque ressources
7. **Tableau de Bord** — Dashboard scores, rapports exports, multi-utilisateurs (admin/collaborateur/lecteur)
8. **Extension Chrome** — Detection fonds, pre-remplissage formulaires, panneau guidage, suivi candidatures

## Conventions de Developpement

### Langue
- Code : anglais (variables, fonctions, classes)
- Commentaires : francais
- UI/UX : francais (interface utilisateur)
- Documentation : francais

### Frontend (Nuxt 4)
- Composition API avec `<script setup lang="ts">`
- Composables dans `composables/`
- Pages dans `pages/` avec routing automatique Nuxt
- Composants dans `components/` avec nommage PascalCase (sans prefixe de dossier, `pathPrefix: false`)
- Stores Pinia dans `stores/`
- Structure Nuxt 4 : tous les fichiers source dans `app/` (pages, components, composables, layouts, stores, etc.)

### Dark Mode (OBLIGATOIRE)
Chaque nouveau composant, page ou layout DOIT etre compatible dark mode :
- Utiliser les variantes `dark:` de Tailwind sur tous les elements visuels
- Fonds : `bg-white dark:bg-dark-card`, `bg-surface-bg dark:bg-surface-dark-bg`
- Textes : `text-surface-text dark:text-surface-dark-text`, `text-gray-600 dark:text-gray-400`
- Bordures : `border-gray-200 dark:border-dark-border`
- Inputs : `dark:bg-dark-input dark:text-surface-dark-text`
- Hover : `hover:bg-gray-50 dark:hover:bg-dark-hover`
- Le theme est gere par `stores/ui.ts` (classe `dark` sur `<html>`, persiste dans localStorage)
- Les variables de couleurs dark sont definies dans `app/assets/css/main.css` via `@theme`
- Ne jamais hardcoder des couleurs claires sans leur equivalente dark

### Reutilisabilite des Composants (OBLIGATOIRE)
- Avant de creer un nouveau composant, verifier si un composant existant peut etre reutilise ou etendu via des props
- Extraire les patterns visuels repetes (cartes, formulaires, boutons, inputs) en composants generiques dans `components/ui/`
- Les composants UI de base (boutons, inputs, badges, modals) doivent etre parametrables via props et slots, pas dupliques
- Privilegier la composition (slots, props, emit) plutot que la duplication de code
- Si un meme pattern apparait plus de 2 fois, l'extraire en composant reutilisable

### Backend (FastAPI)
- Routers dans `routers/`
- Services/logique metier dans `services/`
- Modeles SQLAlchemy dans `models/`
- Schemas Pydantic dans `schemas/`
- snake_case pour les fonctions et variables Python

### Base de Donnees
- Migrations avec Alembic
- Nommage tables : snake_case, pluriel (ex: `companies`, `esg_scores`)
- pgvector pour les embeddings de documents

## Contexte Metier

### Public Cible
- PME africaines francophones (zone UEMOA/CEDEAO)
- Secteurs : agriculture, energie, recyclage, transport, etc.
- Secteur informel pris en compte

### Referentiels ESG
- Taxonomies vertes UEMOA, BCEAO
- Reglementations CEDEAO
- Standards internationaux : Gold Standard, Verra, REDD+

### ODD Cibles
- ODD 8 (Travail decent), ODD 9 (Innovation), ODD 10 (Inclusion financiere)
- ODD 12 (Production responsable), ODD 13 (Climat), ODD 17 (Partenariats)

## Environnement Python

Le backend utilise un environnement virtuel Python (`venv`). Toujours l'activer avant d'executer des commandes Python ou d'installer des packages.

```bash
# Creation (une seule fois)
cd backend && python3 -m venv venv

# Activation (a chaque session)
source backend/venv/bin/activate

# Installation des dependances
pip install -r backend/requirements.txt
```

**Important** : Ne jamais installer de packages Python globalement. Toujours verifier que le venv est actif (`which python` doit pointer vers `backend/venv/bin/python`).

## Commandes Utiles

```bash
# Frontend
cd frontend && npm run dev

# Backend (toujours activer le venv d'abord)
source venv/bin/activate
uvicorn app.main:app --reload

# Base de donnees
alembic upgrade head
```

## Conventions

- **Français avec accents** (é, è, ê, à, ç, ù) obligatoires dans le les contenus

## Active Technologies
- Python 3.12, TypeScript 5.x (strict mode) (001-technical-foundation)
- PostgreSQL 16 + pgvector (001-technical-foundation)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) (002-chat-rich-visuals)
- PostgreSQL 16 + pgvector (async via asyncpg) (002-chat-rich-visuals)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph, LangChain, SQLAlchemy (async), Nuxt 4, Vue Composition API, Pinia, TailwindCSS (003-company-profiling-memory)
- PostgreSQL 16 + pgvector (async via asyncpg), Alembic pour migrations (003-company-profiling-memory)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph, LangChain, SQLAlchemy (async), PyMuPDF, pytesseract, pdf2image, docx2txt, openpyxl, Nuxt 4, Vue Composition API, Pinia, TailwindCSS (004-document-upload-analysis)
- PostgreSQL 16 + pgvector (embeddings), stockage fichiers local (/uploads/) (004-document-upload-analysis)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph, LangChain, SQLAlchemy async, PyMuPDF, Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Chart.js, vue-chartjs (005-esg-scoring-assessment)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, WeasyPrint, matplotlib, Jinja2, LangChain (resume IA), SQLAlchemy async, Nuxt 4, Vue Composition API, Pinia, TailwindCSS (006-esg-pdf-reports)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, Claude API via OpenRouter (backend) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Chart.js, vue-chartjs (frontend) (007-carbon-footprint-calculator)
- PostgreSQL 16 + pgvector, Alembic pour migrations (007-carbon-footprint-calculator)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, WeasyPrint, Jinja2 (backend) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Chart.js, vue-chartjs (frontend) (008-green-financing-matching)
- PostgreSQL 16 + pgvector (embeddings), Alembic pour migrations (008-green-financing-matching)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, WeasyPrint, Jinja2, python-docx (backend) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, toast-ui/editor, Chart.js (frontend) (009-fund-application-generator)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, WeasyPrint (backend) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Chart.js, vue-chartjs (frontend) (011-dashboard-action-plan)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangChain (>=0.3.0), LangGraph (>=0.2.0), langchain-openai (>=0.3.0), SQLAlchemy async (012-langgraph-tool-calling)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph (>=0.2.0), LangChain (>=0.3.0), langchain-openai (>=0.3.0), SQLAlchemy async, Nuxt 4, Vue Composition API (013-fix-multiturn-routing-timeline)
- PostgreSQL 16 + pgvector, MemorySaver (LangGraph checkpointer) (013-fix-multiturn-routing-timeline)
- Python 3.12 + FastAPI, LangGraph, LangChain, langchain-openai (014-concise-chat-style)
- N/A (pas de changement BDD) (014-concise-chat-style)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph (>=0.2.0), LangChain (>=0.3.0), langchain-openai, SQLAlchemy async (015-fix-toolcall-esg-timeout)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph (>=0.2.0), LangChain (>=0.3.0), langchain-openai, SQLAlchemy async, Nuxt 4, Vue Composition API (016-fix-tool-persistence-bugs)
- Python 3.12 + FastAPI, pytest, pytest-asyncio, LangChain, LangGraph (017-fix-failing-tests)
- PostgreSQL + pgvector (SQLite in-memory pour tests) (017-fix-failing-tests)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, LangGraph, LangChain, SQLAlchemy async, Pydantic v2 ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (018-interactive-chat-widgets)
- PostgreSQL 16 (JSONB), Alembic, table satellite `interactive_questions` (aucune modification des tables existantes) (018-interactive-chat-widgets)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Playwright (019-multitenant-roles-rls)
- PostgreSQL 16 + pgvector, Alembic migration 019, Row-Level Security (ENABLE+FORCE) sur 14 tables metier, tables `accounts`/`refresh_tokens`/`account_invitations`, colonnes `account_id` UUID NOT NULL FK + 2 policies (`pme_access_own_account`, `admin_full_access`) (019-multitenant-roles-rls)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, langchain-openai (embeddings) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (020-fondations-sourcage-catalogue / F01)
- PostgreSQL 16 + pgvector, Alembic migration 020 (11 nouvelles tables : `sources`, `indicators`, `criteria`, `formulas`, `thresholds`, `referentials`, `referential_indicators`, `emission_factors`, `required_documents`, `simulation_factors`, `unsourced_flags`), workflow 4-yeux (CHECK contraint captured_by != verified_by), RLS PostgreSQL en lecture publique sur entites verified/published (020-fondations-sourcage-catalogue / F01)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async (event listeners before_flush), Alembic, Pydantic v2 ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (021-audit-log / F03)
- PostgreSQL 16 + pgvector, table `audit_log` strictement append-only (triggers PL/pgSQL BEFORE UPDATE/DELETE qui RAISE EXCEPTION + REVOKE UPDATE,DELETE best-effort), 4 indexes ciblés, 2 ENUMs (`audit_action`, `audit_source`), RLS héritée F02 avec 4 policies (pme_access_own_account, pme_insert_own_account, admin_full_access, admin_insert_anywhere) (021-audit-log / F03)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic, Pydantic v2 strict (Money type) ; httpx (exchangerate-api.com) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (022-versioning-money-devises / F04)
- PostgreSQL 16 + pgvector, table `exchange_rates` (référentiel public global, sans account_id, avec UNIQUE (base, quote, as_of) + CHECK enum currencies), 4 colonnes versioning (`version`, `valid_from`, `valid_to`, `superseded_by`) ajoutées sur 12 tables catalogue + `catalog_version` sur sources (qui possède déjà un champ version métier), trigger PL/pgSQL `prevent_supersede_cycle` avec 13 triggers BEFORE INSERT/UPDATE (skip SQLite), snapshot immuable (`snapshot_at`, `snapshot_data` JSONB) sur fund_applications, paires Money typées (`<field>_amount` Numeric(20,2) + `<field>_currency` Char(3)) sur funds (min/max), company_profiles (annual_revenue), action_items (estimated_cost), backfill XOF idempotent (anciennes colonnes `*_xof` conservées en phase 1), 8 entrées seed `exchange_rates` (USD↔{XOF,EUR,GBP,JPY} + inverses), constante `FCFA_EUR_PEG = Decimal("655.957")` (Banque de France/BCEAO) (022-versioning-money-devises / F04)
- Python 3.12 (backend) ; TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph (AsyncPostgresSaver), LangChain, langchain-openai (text-embedding-3-small), pgvector (HNSW cosine) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (023-memoire-contextuelle-pgvector / F12)
- PostgreSQL 16 + extension pgvector. Nouvelle table `message_chunks` (account_id FK accounts RESTRICT, conversation_id FK conversations CASCADE, message_id FK messages CASCADE, chunk_index INT, role VARCHAR(20), chunk_text TEXT, embedding VECTOR(1536) NULL, created_at TIMESTAMPTZ) avec index composite, partiel `pending_embedding`, HNSW `vector_cosine_ops`, RLS ENABLE+FORCE + 2 policies (admin_full_access, pme_access_own_account). Tables `checkpoints`, `checkpoint_writes`, `checkpoint_blobs` créées automatiquement par `AsyncPostgresSaver.setup()` au lifespan FastAPI. (023-memoire-contextuelle-pgvector / F12)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic, Pydantic v2 ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Playwright (024-carbone-mix-uemoa-source / F17)
- PostgreSQL 16. Migration Alembic `024_carbone_mix_uemoa` (down_revision=`023_create_message_chunks`) : colonne `year` Integer NOT NULL sur `emission_factors` avec backfill 2024, index composite `idx_emission_factors_lookup (category, country, year)`, contrainte UNIQUE `emission_factors_cat_country_year_uniq`, ~33 facteurs F17 seedés (8 pays UEMOA × 2 années + global, 3 combustibles, 7 transport, 3 déchets, 6 achats matières premières) sourcés ADEME Base Carbone v23 / IEA Africa Energy Outlook 2024 / IPCC AR6 WG3, FK `source_id`+`factor_id` UUID NOT NULL sur `carbon_emission_entries` avec backfill matching strict subcategory→code + fallback prefix + fallback générique global, conservation legacy `source_description` (drop reporté ≥ 2 sprints). (024-carbone-mix-uemoa-source / F17)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic, Pydantic v2 (Money typed F04), LangChain, LangGraph ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Playwright (025-entite-projet-vert / F06)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic, Pydantic v2 (Money typed F04), LangChain, LangGraph ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Playwright (028-entite-offre-fonds-intermediaire / F07)
- PostgreSQL 16 + pgvector, Migration Alembic 028 (down_revision=`027_consents_and_deletion`) : nouvelle table `offers` (couple Fonds × Intermédiaire avec versioning F04 + 4 CHECK constraints), enrichissement `funds` (instruments JSONB, theme JSONB, submission_mode, submission_calendar, source_id F01, publication_status, renommage enum fund_type → multilateral|bilateral|regional|national|private|carbon_marketplace), enrichissement `intermediaries` (code unique sparse, required_documents JSONB, fees_structured JSONB, processing_time_days_min/max, disbursement_time_days_min/max, submission_portal_url, success_rate, total_funded_volume Money typed, source_id, publication_status), enrichissement `fund_intermediaries` (accredited_from NOT NULL, accredited_to, max_amount_per_fund Money typed, accreditation_source_id), nouvelle colonne `fund_applications.offer_id` (NOT NULL post-backfill), seed singleton DIRECT (intermediary `code='DIRECT'` + Source `system://mefali/direct-singleton`), backfill complet pour toutes paires fund_intermediaries existantes + tous fonds direct, calculator `compute_effective_offer` (intersection critères avec règle « le plus restrictif gagne », union documents avec dédup exacte sur `(title, source_id)`, somme frais Money typed XOF, somme délais, hint langues anglophones), 6 endpoints REST `/api/offers/*` (public) + `/api/admin/offers/*` (admin-only via `get_current_admin`), 3 tools LangChain (`list_offers`, `get_offer`, `compare_offers_for_fund`) + extension `create_fund_application` (offer_id prioritaire), cron `check_expired_accreditations.py` (idempotent, journalise via `audit_log` source_of_change='import'), feature flag `USE_OFFER_VIEW` (env var Nuxt `NUXT_PUBLIC_USE_OFFER_VIEW`, default false MVP), 8 composants Vue (`OfferCard`, `OfferDetail`, `EffectiveCriteriaList`, `EffectiveDocumentsList`, `EffectiveFees`, `SubmissionModeBadge`, `FundCard`, `IntermediaryCard`) + 4 pages (`/financing/offers/[offer_id]`, `/financing/offers/index` comparateur, `/financing/funds/[fund_id]`, `/financing/intermediaries/[intermediary_id]`), composable `useFinancing` étendu (listOffers, getOffer, compareOffersForFund), types TypeScript exhaustifs, tests backend pytest 65+ (calculator unit 21, model 6, seed 3, router public 9, router admin 10, security 5, cron 5, tools 6) + tests frontend Vitest 39 (8 composants), spec Playwright `frontend/tests/e2e/F07-entite-offre-fonds-intermediaire.spec.ts` (7 scénarios E2E). Round-trip Alembic up/down/up validé sur PostgreSQL. Catalogue exempt du mixin Auditable (cohérent F03 policy). 2035 tests backend verts (1956 baseline + 79 F07 incluant ajustements tests existants), 0 régression. 609 tests frontend verts (3 préexistants en échec hors F07). (028-entite-offre-fonds-intermediaire / F07)
- PostgreSQL 16. Migration Alembic `025_create_projects` (down_revision=`024_carbone_mix_uemoa`) : 2 nouvelles tables (`projects` 21 colonnes avec multi-tenant F02, Auditable F03, Money typed F04 sur `target_amount`, JSONB array `objective_env` ; `project_documents` table de jointure UNIQUE (project_id, document_id)), 1 colonne ajoutée (`fund_applications.project_id` UUID FK NOT NULL après backfill auto-généré), 5 indexes composites/btree, 11 CHECK constraints applicatives portables (status/maturity/financing_structure/objective_env enum + Money pair + bornes ≥ 0), RLS PostgreSQL ENABLE+FORCE + 4 policies (`pme_access_own_account`, `admin_full_access` sur projects ; `pme_access_via_project`, `admin_full_access` sur project_documents), backfill SQL CTE PostgreSQL idempotent + Python loop SQLite pour FundApplication orphelines (`auto_generated=true`, mapping status accepted→funded, autres→seeking_funding). (025-entite-projet-vert / F06)
- PostgreSQL 16 + pgvector (lecture seule pour Source/Project/Offer). Aucune nouvelle table, aucune migration Alembic. (029-tools-visualisation-typed)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, LangGraph, LangChain, WeasyPrint, Jinja2 (backend) ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS (frontend) (030-scoring-multi-referentiels)
- PostgreSQL 16 + pgvector. Migration Alembic 030 (down_revision='028_offers_and_enrich') : nouvelle table `referential_scores` (multi-référentiel scoring + versioning F04 superseded_by + RLS), seed 5 référentiels MVP, backfill ESGAssessment Mefali, ALTER TYPE reminder_type_enum ADD VALUE 'referential_version_evolved'. (030-scoring-multi-referentiels)
- PostgreSQL 16 + pgvector (embeddings non concernés par F10), table `interactive_questions` étendue (colonnes `payload jsonb`, `response_payload jsonb`) (031-widgets-bottom-sheet-complets)
- Python 3.12 (backend), TypeScript 5.x strict (frontend) + FastAPI, SQLAlchemy async, Alembic, LangGraph, LangChain, langchain-openai, tiktoken cl100k_base, semver, Pydantic v2 ; Nuxt 4, Vue Composition API, Pinia, TailwindCSS, Playwright (033-skills-playbooks-metier / F23)
- PostgreSQL 16 + pgvector. Migration Alembic 033 (down_revision='032_add_validation_error_tool_call_logs') : nouvelle table `skills` (18 colonnes avec versioning F04, JSONB tool_whitelist/sources/activation_rules/golden_examples, status draft/published, FK created_by/verified_by RESTRICT avec four_eyes CHECK, superseded_by self-FK SET NULL), 3 indexes (BTREE composite + status + GIN PG-only sur activation_rules), 3 CHECK (domain enum 7 valeurs, status, four_eyes). Skill ajoutée à EXEMPT_MODELS (catalogue admin-only). 3 skills MVP seedées (skill_esg_diagnostic, skill_score_gcf, skill_dossier_gcf_via_boad). (033-skills-playbooks-metier / F23)

## Recent Changes
- 033-skills-playbooks-metier (F23): Skills (Playbooks Métier) - bundles métier réutilisables prompt+procedure+tools+sources+golden_examples avec eval gating publication. Migration Alembic `033_create_skills` (down_revision=`032_add_validation_error_tool_call_logs`) : nouvelle table `skills` 18 colonnes (id UUID PK, name VARCHAR(100) UNIQUE, domain VARCHAR(50), version VARCHAR(50) default `1.0.0`, prompt_expert TEXT ≤ 5000 tokens, procedure TEXT ≤ 3000 tokens, tool_whitelist JSONB, sources JSONB, activation_rules JSONB, golden_examples JSONB, status VARCHAR(20) draft|published, created_by FK users RESTRICT, verified_by FK users RESTRICT NULLABLE, valid_from/valid_to DATE, superseded_by self-FK SET NULL, created_at/updated_at), 3 CHECK constraints (domain enum 7 valeurs, status enum, four_eyes verified_by != created_by), 3 indexes (composite domain/status/valid_to, status, GIN PG-only sur activation_rules). 3 skills MVP seedées : skill_esg_diagnostic, skill_score_gcf, skill_dossier_gcf_via_boad. Module `app/modules/skills/` complet (schemas Pydantic v2, validator anti-injection 10 patterns OWASP + tokens cap tiktoken cl100k_base + tool whitelist + sources verified, service CRUD avec versioning F04 patch+1 sur édition published, eval_runner asyncio.gather max 5 concurrent + timeout 60s + DRY `app/lib/eval_matching.py` partagé F22, seed idempotent, exceptions typées). Helpers LangGraph `app/graph/skill_loader.py` (load_skills_for_context multi-critères score offer_id 4 > fund+intermediary 3 > active_module 1.5 > page_slug 1 > intent_keywords 0.5, top 2) + `app/graph/prompt_fusion.py` (fuse_prompt avec sections SKILL ACTIVE + cap budget 12k tokens, select_tools_with_skills intersection, SkillToolMismatchError + fallback) + `app/graph/skill_integration.py` (helper apply_skills_to_node tolérant aux erreurs avec bypass safe). Refactor 7 nœuds LangGraph (chat, esg_scoring, carbon, financing, application, credit, action_plan) avec snapshot `state["active_skills"]`. Champ `active_skills: list[dict] | None` ajouté à `ConversationState`. Helper `app/core/prompt_injection_detector.py` 10 patterns regex (ignore_previous_instructions, new_role, system_prompt_leak, user_is_admin, forget_everything, override_instructions, system_tag, developer_mode, jailbreak_keywords, prompt_extraction). Conformity test `tests/graph/tools/test_no_skill_mutation_tool.py` garde-fou bloquant : aucun tool LLM ne mute Skills (pattern interdit `^(create|update|delete|publish|unpublish)_skill`). Router admin `app/modules/admin/skills_router.py` 8 endpoints REST `/api/admin/skills/*` protégés par `Depends(get_current_admin)` : GET liste paginée filtres domain/status/q, POST create, GET detail, PATCH update (in-place draft ou nouvelle version draft 1.0.x si published), POST publish (gating eval ≥ 90 % seuil sinon 422), POST test (eval sans publication), POST unpublish, DELETE soft (drafts uniquement, valid_to=today). Script CLI `scripts/seed_skills.py` (idempotent). Frontend admin : composable `useAdminSkills.ts` 8 méthodes, types TypeScript `types/skills.ts` exhaustifs, 7 composants `components/admin/skills/` (SkillList paginée + filtres + actions, SkillForm 8 onglets avec compteur tokens temps réel, ToolWhitelistPicker chips, SourceMultiPicker, ActivationRulesEditor inputs CSV, GoldenExamplesEditor 5-15 cas guidés, SkillEvalRunner bouton Tester + affichage rapport gate_passed/failed_cases), 3 pages `/admin/skills/{index, new, [id]}` avec dark mode complet. Spec Playwright `frontend/tests/e2e/admin/skills.spec.ts` 3 scénarios (créer skill draft, publier sans 5 examples, lister). Skill ajoutée à `EXEMPT_MODELS` (catalogue admin-only sans account_id, audit log via middleware AdminAuditContextMiddleware). Documentation `docs/skills-playbooks.md` (cycle de vie complet, anti-injection guidelines, eval gating, versioning, dépannage). Couverture ≥ 80 %. 2693 tests backend verts (2632 baseline + 61 F23 incluant 10 model + 29 anti-injection + 11 eval_matching + 13 skill_loader + 12 prompt_fusion + 17 conformity + 4 integration + 10 validator + 6 eval_runner + 11 service + 3 seed + 10 admin router + 3 publish_e2e), 0 régression. 731 tests frontend verts (734 baseline avec 3 préexistants en échec hors F23). Round-trip Alembic `up/down/up` validé sur PostgreSQL.
- 030-scoring-multi-referentiels (F13): Module scoring ESG multi-référentiels. Migration Alembic `030_create_referential_scores` (down_revision='028_offers_and_enrich') : nouvelle table `referential_scores` (16 colonnes, FK account_id CASCADE/assessment_id CASCADE/referential_id RESTRICT, superseded_by self-référent SET NULL, pillar_scores/covered_criteria/missing_criteria JSONB, coverage_rate Numeric(4,3) avec CHECK [0,1], computed_by ENUM manual/llm/auto), index unique partiel `idx_referential_scores_current WHERE superseded_by IS NULL` + 3 indexes secondaires + RLS PostgreSQL ENABLE+FORCE + policy `referential_scores_account_isolation`, ajout valeur `referential_version_evolved` à `reminder_type_enum` (F11 reuse), seed 5 référentiels MVP idempotent (Mefali UUID stable + GCF + IFC PS + BOAD ESS + GRI 2021) avec sources `system://mefali/referential-<code>`, backfill complet ESGAssessment finalisés → referential_scores Mefali (idempotent ON CONFLICT). Modèle SQLAlchemy `ReferentialScore` (Auditable EXEMPT — artefact calculé). Schémas Pydantic v2 stricts (`PillarScore`, `CoveredCriterion`, `MissingCriterion`, `ReferentialScoreRead`, `ComparisonResult`, `RecomputeRequestResponse`, `FinalizeAssessmentResult`, `BottleneckInfo`, `DualReferentialResponse`, `GenerateReportRequest`). Service `app/modules/esg/multi_referential_service.py` avec `compute_score_for_referential` (pondération qui ignore les non-renseignés, coverage_rate, source_id F01 par critère, fallback Mefali sur les 30 critères legacy F05), `compute_all_referential_scores` (asyncio.gather parallèle, UPSERT idempotent avec versioning F04 superseded_by, atomicité par référentiel via return_exceptions, mise à jour des colonnes legacy `esg_assessments.overall_score|environment_score|...` 2 sprints), `compute_referential_score_for_offer` (fallback Mefali si fund.referential_id IS NULL, identification du goulot d'étranglement min(fund_score, intermediary_score) + top 3 critères), `recompute_score_async` (helper FastAPI BackgroundTasks). 3 tools LangChain dans `app/graph/tools/esg_tools.py` (`finalize_esg_assessment_multi_ref` avec `referentials_to_compute: list[str]`, `recompute_score(entity_id, referentiel_id)`, `compare_referentials(assessment_id, referentials)`) avec docstrings 5-sections conformes story 10.1. 3 endpoints REST `/api/esg/assessments/{id}/referential-scores` (GET courants), `/referential-scores/history?referential_id=X` (GET supersédés), `/recompute-score?referentiel_id=X` (POST 202 + recompute_request_id, BackgroundTasks). Endpoint `POST /api/reports/esg/{id}/generate` refactorisé : accepte body `{referentials, include_appendix_sources}` (defaults `["mefali"]`/`true` pour rétrocompat F06), validation 422 si codes invalides avec liste valid_codes. Partial Jinja2 `_appendix_sources.html` pour annexe « Sources et références » F01. Cron `scripts/check_referential_versions_evolution.py` détecte évolution version + crée reminders F11 `kind='referential_version_evolved'` (idempotent). Frontend : 6 composants Vue dans `components/esg/` (`ReferentialSelector` ARIA listbox, `ReferentialScoreCard` avec ScoreCircle + badge orange < 50% + bouton Inclure dans rapport PDF désactivé, `MissingCriteriaList` modale détail + SourceLink F01, `BottleneckBanner` couleur rouge/jaune/vert selon eligibility + bouton Renseigner maintenant emit `focus-indicators`, `DualReferentialView` grid responsive desktop/mobile avec gestion fallback, `MultiReferentialReportModal` checkboxes dynamiques + désactive coverage < 50%) avec dark mode complet (`dark:bg-dark-card`, `dark:border-dark-border`, `dark:text-surface-dark-text`). Composable `useEsgMultiReferential.ts` (getReferentialScores, getReferentialScoresHistory, recomputeScore, pollReferentialScores polling 2s timeout 30s, generateMultiReferentialReport). Store Pinia `esg.ts` étendu : `referentialScores`, `selectedReferential`, `isRecomputing`, `recomputeRequestId` + getters `currentReferentialScore`, `scoresWithCoverageOk`. Types TypeScript `ReferentialScore`, `PillarScore`, `CoveredCriterion`, `MissingCriterion`, `ComparisonResult`, `RecomputeRequestResponse`, `BottleneckInfo`, `DualReferentialResponse`. Refactor page `/esg/results.vue` avec sélecteur multi-réf + carte score + liste critères + modale rapport. Tests backend : 47 nouveaux tests (4 model + 17 schemas + 14 migration + 10 multi_referential_service + 6 router + 9 tools + 4 cron + 4 security RLS + 1 legacy equality + 4 reports validation), 0 régression. Tests frontend : 30 nouveaux tests Vitest (5 ReferentialSelector + 9 ReferentialScoreCard + 6 MissingCriteriaList + 7 BottleneckBanner + 3 DualReferentialView), 0 régression. Spec Playwright `frontend/tests/e2e/F13-scoring-multi-referentiels.spec.ts` (5 scénarios US1/US2/US3 avec mocks SSE). Couverture 100% sur model + schemas, 50% sur multi_referential_service (compute_referential_score_for_offer skipped en SQLite test, recompute_score_async non testable directement). Round-trip Alembic `up/down/up` validé sur PostgreSQL. UUID Mefali stable `0e5f1310-1310-1310-1310-13101310f013` (évite edge case sérialisation SQLAlchemy 2.0 UUID avec int=1).
- 029-tools-visualisation-typed (F11): Tools de visualisation typés (KPICard, MatchCard, Map, ComparisonTable). 4 tools LangChain Pydantic strict (`extra='forbid'`, bornes, enums fermés) dans `app/graph/tools/visualization_tools.py` : `show_kpi_card` (chiffre clé sourcé F01 + Money typé F04 + delta colorisé), `show_match_card` (projet F06 ↔ offre F07 cliquable avec score circulaire et drilldown vers `/financing/offers/{offer_id}?project_id={project_id}`), `show_map` (Leaflet UEMOA avec markers SVG colorés par type project/intermediary/fund_office/company_hq + overlay GeoJSON UEMOA bundlé local ~8 KB), `show_comparison_table` (2-5 sujets côte-à-côte avec validateur cross-field values↔subjects + highlight winner par row + responsive cartes mobile). Schémas Pydantic centralisés dans `app/schemas/visualization.py` (8 modèles : `KPICardArgs`, `MatchCardArgs`, `MapMarker`, `MapArgs`, `ComparisonValue`, `ComparisonRow`, `ComparisonSubject`, `ComparisonTableArgs`). Constante `app/core/visualization_centroids.py` (`UEMOA_COUNTRY_CENTROIDS` 8 entrées hardcodées BEN/BFA/CIV/GNB/MLI/NER/SEN/TGO + `UEMOA_REGION_CENTER` + `UEMOA_DEFAULT_ZOOM`). Validator `app/graph/validators/payload_invalid.py` (`PayloadInvalidResult` immuable, `build_llm_error_message`, `handle_payload_invalid`) avec retry max 1 + fallback texte. Transport SSE via marker `<!--SSE:{"__sse_visualization_block__":true,...}-->` détecté par `stream_graph_events` → événement typé `visualization_block` (block_type, payload). Mise à jour `tool_selector_config.py` : visibilité par page (KPICard sur dashboard/esg/carbon/credit, MatchCard sur financing/candidatures, Map sur profile/profile_projects/financing, ComparisonTable sur financing/candidatures) + binding dans 7 noeuds (`chat`, `esg_scoring`, `carbon`, `financing`, `application`, `credit`, `action_plan`). System prompt enrichi avec section "ARBRE DE DÉCISION VISUALISATION" (7 cas) ; prompts `financing.py` et `application.py` complétés avec section "TOOLS DE VISUALISATION TYPÉS - PRIORITAIRES". Frontend : ajout `leaflet@^1.9.4` + `@types/leaflet@^1.9.12`, asset `frontend/app/assets/geo/uemoa-borders.geo.json` (~8 KB, 8 polygones simplifiés Public Domain Natural Earth). 4 composants Vue dans `frontend/app/components/richblocks/` (`KPICardBlock` avec gradient color emerald/blue/rose/amber/violet + delta vert/rouge selon `deltaIsGood` + SourceLink F01 + drilldown role=link tabindex 0, `MatchCardBlock` avec 2 logos placeholder/initiales + score circulaire SVG + tooltip breakdown + badges instruments + bouton CTA, `MapBlock` chargé en lazy-load via `defineAsyncComponent` avec dynamic import Leaflet en `onMounted` + DOMPurify sur popups + marker SVG divIcon coloré par type, `ComparisonTableBlock` avec table desktop + cartes mobile collapse responsive ≤768px + format par type money/percentage/duration/rating/boolean + highlight winner via parsing best-effort + SourceLink par cellule). Composable `useMapTiles` (computed tileUrl + attribution réactif au store ui : OSM standard light, CartoDB Dark Matter dark, sans clé API). Types TypeScript miroir `frontend/app/types/richblocks.ts` (camelCase : `KPICardBlockProps`, `MatchCardBlockProps`, `MapBlockProps`, `MapMarkerProps`, `ComparisonTableBlockProps`, `ComparisonRowProps`, `ComparisonValueProps`, `ComparisonSubjectProps` + enums `DeltaDirection`, `KPIColor`, `MarkerType`, `ComparisonValueType`). Extension `useChat.ts` : `visualizationBlocksByMessage` Record<msgId, VisualizationBlock[]> avec migration tempId → realId, propagation dans `MessageParser` via `ChatMessage.vue` et `FloatingChatWidget.vue`. `MessageParser.vue` étendu avec helper `toCamelCase` (préserve Money typé) et rendu lazy de MapBlock via `defineAsyncComponent`. Aucune migration Alembic, aucun nouvel endpoint REST (transport SSE via tool calling 012). Couverture backend 100 % sur les 4 modules F11 (`visualization_tools`, `schemas/visualization`, `core/visualization_centroids`, `validators/payload_invalid`) ; couverture frontend ≥ 80 % sur les 4 composants richblocks + composable. 171 nouveaux tests backend + 65 nouveaux tests frontend Vitest. Spec Playwright `F11-tools-visualisation-typed.spec.ts` (4 scénarios skippés en phase B, validation phase C). 2206 tests backend verts (2035 baseline F07 + 171 F11), 0 régression. 674 tests frontend verts (609 baseline + 65 F11, 3 préexistants en échec hors F11).
- 025-entite-projet-vert (F06): Entité Projet Vert (Module 1.3). Nouvelle entité `Project` matérialisant le triangle conceptuel `Entreprise 1—N Projets 1—N Candidatures vers Offres = (Fonds × Intermédiaire)`. Modèle SQLAlchemy `Project` (Auditable F03 + UUIDMixin + TimestampMixin) avec multi-tenant `account_id` F02, Money typed F04 sur `target_amount` (paire amount/currency + CHECK pair), JSONB array `objective_env` (whitelist 8 valeurs : mitigation/adaptation/biodiversity/circular_economy/water/renewable_energy/sustainable_agriculture/mixed), maturity (5 valeurs : ideation/pre_feasibility/pilot/scale/replication), status (6 valeurs : draft/seeking_funding/funded/in_execution/closed/cancelled), financing_structure (5 valeurs : subvention/pret_concessionnel/equity/blending/mixte), expected_impact_tco2e/jobs/beneficiaries/hectares/other_impacts JSONB, location_country/region (PostGIS différé F11), flag `auto_generated` pour migration backfill. Modèle `ProjectDocument` table de jointure (UNIQUE project_id+document_id, ondelete CASCADE, doc_type 5 valeurs : feasibility_study/business_plan/impact_assessment/support_letter/other). Ajouté `Project` à `AUDITABLE_MODELS` ; `ProjectDocument` à `EXEMPT_MODELS`. Migration Alembic 025 réversible (up/down/up) avec backfill auto-généré pour FundApplication orphelines (mapping accepted→funded, autres→seeking_funding, name « Projet hérité — <fund.name> (<YYYY-MM>) » tronqué 200 chars), application NOT NULL en deuxième temps, RLS ENABLE+FORCE + 4 policies. Module métier complet `app/modules/projects/` (service async avec list/get/create/update/soft_delete/duplicate/link_document/list_applications/get_active_projects_for_user, schemas Pydantic v2 strict avec validators enum portables et model_validator Money pair, router REST 7 endpoints `/api/projects/*` protégés par `Depends(get_current_user)` avec RLS PG via `app.current_account_id`). Refus dur de suppression avec applications actives (status NOT IN rejected/accepted/cancelled) → 409 Conflict avec liste blocked_by + hint, paramètre `force=true` pour soft-delete (status=cancelled, log structuré INFO `project_force_deleted`). Duplication force `status=draft` + suffix `' (copie)'` automatique tronqué 200 chars + project_documents NON copiés (FR-016). 7 tools LangChain dans `app/graph/tools/project_tools.py` (`list_projects`/`get_project`/`create_project`/`update_project`/`delete_project`/`duplicate_project`/`link_document_to_project`) avec `args_schema` Pydantic strict, `source_of_change_scope('llm')` automatique sur mutations, sérialisation JSON, gestion erreurs `{ok: false, error}`. Tools injectés : `list_projects` dans `MODULE_TOOL_MAPPING['chat']` + `PAGE_TOOL_MAPPING['chat_global']` ; lecture (`list_projects`+`get_project`) dans `PAGE_TOOL_MAPPING['profile']` ; 7 tools exclusifs dans `PAGE_TOOL_MAPPING['profile_projects']` ; pattern URL `^/profile/projects(?:/|$)` ajouté AVANT `/profile` dans `_PATH_TO_SLUG_PATTERNS` (l'ordre compte). PROJECT_TOOLS injecté dans le ToolNode `chat` (`graph.py`) et `bind_tools` du chat_node (`nodes.py`). `_load_full_context_for_state(db, user_id)` ajoutée dans `app/api/chat.py` retournant `{profile, projects}` (projets actifs status≠cancelled/closed limit 20), injectée dans le state LangGraph via paramètre `user_projects`. Champ `user_projects` ajouté à `ConversationState`. Prompts `application.py` et `financing.py` enrichis avec section « PROJET CIBLE — OBLIGATOIRE AVANT CANDIDATURE » instruisant le LLM d'appeler `list_projects` avant `create_fund_application` et de passer par `ask_interactive_question` si aucun projet n'existe. Frontend : 6 composants Vue dans `components/projects/` (`ProjectCard`, `ProjectForm` mode create/edit/duplicate, `ProjectList` avec pagination/empty state, `ProjectImpactBadges` ARIA role list, `ProjectStatusSelector` ARIA combobox+listbox, `ProjectFilters` ARIA search, `DuplicateProjectModal` avec useFocusTrap + ARIA dialog) avec dark mode complet (`dark:bg-dark-card`, `dark:border-dark-border`, `dark:text-surface-dark-text`, `dark:hover:bg-dark-hover`). 4 nouvelles pages : `pages/profile.vue` refactorisée en page tabs (Entreprise/Mes Projets) avec redirection auto vers `/profile/company`, `pages/profile/company.vue` (contenu profil entreprise déplacé), `pages/profile/projects/index.vue` (liste paginée + filtres URL-synchronisés), `pages/profile/projects/new.vue` (création), `pages/profile/projects/[id].vue` (édition + duplication + suppression avec dialog blocked_by + force), `pages/profile/projects/[id]/duplicate.vue` (duplication dédiée). Composable `composables/useProjects.ts` (8 méthodes : listProjects/getProject/createProject/updateProject/deleteProject/duplicateProject/linkDocument/getProjectApplications) avec `useFetchAuth` Bearer token, gestion 404/409/422. Store Pinia `stores/projects.ts` avec state (projects/total/currentProject/filters/loading/error), getters `activeProjects` (excl. cancelled/closed), `archivedProjects`, `activeCount`, `byStatus(status)`, mutations addProject/updateProjectInList/removeFromList/reset. Types TypeScript `types/project.ts` exhaustifs (8 types unions + 7 whitelists + 6 dictionnaires labels + 12 interfaces). Lien sidebar « Mes Projets » avec icône arbre/feuille verte ajouté à `AppSidebar.vue`. 108 tests backend (model 16, schemas 26, service 17, router 17, tools 17, tool_selector 7, load_full_context 3) avec couverture 87 % sur les modules F06 (project_tools 83%, models project/project_document 100%, modules/projects router 86%/schemas 94%/service 82%) + 56 tests frontend Vitest (useProjects 12, store projects 9, ProjectCard 8, ProjectStatusSelector 8, ProjectImpactBadges 7, ProjectFilters 4, ProjectForm 8) + spec Playwright `frontend/tests/e2e/F06-entite-projet-vert.spec.ts` (4 scénarios US1/US3/US4/US1+US2 avec helpers `F06-helpers.ts` mock backend complet). 1782 tests backend verts (1674 baseline + 108 F06), 0 régression. 509 tests frontend verts (453 baseline + 56 F06, 3 préexistants en échec hors F06). Round-trip Alembic `up/down/up` validé sur PostgreSQL.
- 024-carbone-mix-uemoa-source (F17): Migration des facteurs d'émission carbone codés en dur (Python) vers la table BDD `emission_factors` créée par F01, avec sourçage obligatoire (ADEME Base Carbone v23, IPCC AR6 WG3, IEA Africa Energy Outlook 2024). Mix électrique 8 pays UEMOA (CI/SN/BF/ML/NE/BJ/TG/GW) + variantes 2023 pour fallback année antérieure. Refactor `CarbonEmissionEntry` : ajout `source_id` + `factor_id` UUID FK NOT NULL (en PostgreSQL après backfill), conservation legacy `source_description` 2 sprints. Service `app/modules/carbon/factor_service.py` avec dataclass `EmissionFactorResolution`, exception `EmissionFactorNotFoundError` et fonction async `get_emission_factor(db, category, country, year)` priorité (1) country exact + year exact, (2) country exact + year antérieure récente (diff ≤ 3 = approximate=False, > 3 = True), (3) global + year exact, (4) global + year antérieure. Service `app/modules/carbon/seed_factors.py` avec `SEED_DATA` (~33 facteurs : 8 pays UEMOA × 2 années + global pour électricité, 3 combustibles, 7 transport découpé par carburant/mode, 3 déchets, 6 achats matières premières) et `seed_emission_factors(db, admin_user_id) -> SeedResult` idempotent (SELECT before INSERT par code). Schema Pydantic `app/modules/carbon/reduction_plan_schema.py` : `ReductionPlanAction` (title/description/estimated_reduction_tco2e/cost_estimate_fcfa/timeline/source_id/unsourced) avec validateur `model_validator` qui rejette les combinaisons incohérentes (source_id=null + unsourced=False, ou source_id non null + unsourced=True), `ReductionPlan` conteneur d'`actions[]`. Élargissement `VALID_CATEGORIES` (`backend/app/models/carbon.py`) avec `purchases`. Élargissement `EMISSION_CATEGORIES` (`backend/app/modules/carbon/emission_factors.py`) avec entry `purchases` (label='Achats', applicable_sectors=manufacturing/construction/commerce/mining/...). Tool `save_emission_entry` refactoré : (1) lit `country` du profil entreprise via `get_profile`, (2) appelle `get_emission_factor(db, category=lookup_category, country=country, year=assessment.year)` avec fallback subcategory→category, (3) stocke `source_id` + `factor_id` dans l'entrée, (4) retourne JSON enrichi (`factor_used`, `source_id`, `is_approximate`, `fallback_reason`) pour permettre au LLM d'appeler `cite_source(source_id)`. Service `complete_assessment` valide le `reduction_plan` via `ReductionPlan.model_validate` quand le schema F17 (`actions[]`) est détecté ; format legacy (`quick_wins`/`long_term`) accepté en passe-passe pour rétro-compatibilité. Prompt `CARBON_PROMPT` enrichi : section catégorie Achats détaillée, règle F17 sourçage obligatoire (cite_source), guide sur is_approximate (year_older/country_global), table des valeurs UEMOA 2024 + matières premières ADEME, schema canonique reduction_plan avec contraintes Pydantic. Endpoint admin `POST /api/admin/carbon/seed-factors` (`Depends(get_current_admin)`, retourne `SeedFactorsResponse(inserted, skipped, total_in_db)`). Frontend : composant Vue `EmissionFactorBadge.vue` (props `factor`/`source`/`isApproximate`/`fallbackReason`, structure inline-flex avec label + valeur + `<SourceLink>` cliquable + picto warning amber conditionnel, dark mode complet, ARIA `role="region"` + `aria-label="Facteur d'emission : <label>"`, picto warning `role="img"` + `aria-label="Facteur approximatif"` + tooltip contextuel year_older/country_global), types TypeScript étendus (`EmissionCategory` ajoute 'purchases', `ReductionPlanActionV2` schema canonique F17, `ReductionPlan` accepte `actions[]` OU legacy `quick_wins/long_term`), page `/carbon/results` mise à jour : section plan de réduction rendue avec nouveau schema (titre/description/timeline/coût + `<SourceLink>` cliquable ou badge "Recommandation générale (non sourcée)"), branche legacy `quick_wins`/`long_term` conservée 2 sprints, ajout `purchases` dans `categoryLabels` et `categoryColors`. Backfill migration : matching strict `subcategory = ef.code`, puis prefix LIKE `subcategory + '_%'` (ex. 'electricity_ci' → 'electricity_ci_2024'), puis fallback générique par catégorie (energy → electricity_global_2024, transport → transport_personal_diesel_2024, waste → waste_landfill_global_2024, industrial/agriculture/purchases → matières premières). Adaptation legacy F01 `migration_helpers.seed_emission_factors` : utilise category dérivée `<category>_<code>` pour respecter UNIQUE (category, country, year) tout en conservant les EMISSION_FACTORS Python pour rétro-compatibilité tests F01. 88 nouveaux tests : 11 unit factor_service (match exact/year_older/country_global/draft filtré/not_found), 21 unit seed_factors (structure SEED_DATA + comportement seed/idempotence/source mapping), 15 unit reduction_plan_schema (cohérence source_id↔unsourced + bornes), 8 unit carbon_tools_f17 (country profile + factor_used + source_id pour cite_source + purchases + approximate flag), 18 integration carbon_pipeline_f17 (8 pays UEMOA paramétrés SC-004 + purchases + reduction_plan validation), 15 migration test_alembic_f17 (structure module + helpers + invariants). 10 nouveaux tests Vitest EmissionFactorBadge (label+valeur, SourceLink forwarding, warning conditionnel, dark mode, ARIA, événements). Spec Playwright `frontend/tests/e2e/F17-carbone-mix-uemoa-source.spec.ts` (4 scénarios : CI électricité 0.456, SN distinct, Achats ciment dans ventilation, SourceLink cliquable plan réduction). Couverture : 100 % factor_service, 100 % reduction_plan_schema, 97 % seed_factors, 82 % carbon_tools. 1674 tests backend verts (1586 baseline + 88 F17), 0 régression. Round-trip Alembic `up/down/up/down/up` validé sur PostgreSQL.
- 023-memoire-contextuelle-pgvector (F12): Mémoire contextuelle conforme. Migration Alembic `023_create_message_chunks` (down_revision='022_money_and_versioning') : table `message_chunks` (UUID PK, account_id FK accounts RESTRICT, conversation_id FK conversations CASCADE, message_id FK messages CASCADE, chunk_index INT default 0, role VARCHAR(20) CHECK IN ('user','assistant','system'), chunk_text TEXT, embedding VECTOR(1536) NULL, created_at TIMESTAMPTZ default now()), 3 indexes (composite `(account_id, conversation_id, created_at DESC)`, partiel `(created_at) WHERE embedding IS NULL` pour rattrapage F19, HNSW `vector_cosine_ops` m=16 ef=64), RLS PostgreSQL ENABLE+FORCE + 2 policies (admin_full_access, pme_access_own_account compatibles F02 via `current_setting('app.current_*')`). `MessageChunk` ajouté à `EXEMPT_MODELS` (cache d'embedding, pas une entité métier — la traçabilité est sur Message). Module `app/modules/memory/` complet : `service.py` (`mask_secrets` ordre tokens→email→IBAN→cartes Luhn validées + idempotence + marqueurs `[TOKEN]/[EMAIL]/[BANK]/[CARD]`, `chunk_text` 6000c+overlap 200c par paragraphes/phrases/mots, `embed_message` async best-effort avec fallback embedding=NULL si API échoue, `search_history` SQL pgvector `<=>` cosine + filtre RLS account_id + threshold 0.6 + exclude conversation courante par défaut, `purge_account_chunks` cascade message_chunks + checkpoints LangGraph thread_id), `hooks.py` (event listener `after_insert` sur Message → `asyncio.create_task(embed_message)` si event loop actif, no-op silencieux sinon, set `_BACKGROUND_TASKS` pour éviter GC précoce). Tool LangChain `recall_history(query, max_results, since, include_current_conversation)` dans `app/graph/tools/memory_tools.py` (Pydantic `RecallHistoryArgs` 2..500c query, max 1..10 résultats, since ISO 8601 optionnelle, include_current=False par défaut, hard cap server-side 10) avec helper `_format_relative_time` français court (« à l'instant » / « il y a N min/h/j » / « hier » / « le DD/MM/YYYY ») et log structuré `recall_history_invoked` (FR-028, SC-010). `MEMORY_TOOLS` ajouté à `GLOBAL_WHITELIST` (transverse, 4 globaux désormais : ask_interactive_question, trigger_guided_tour, sourcing×3, recall_history) et borne `MAX_TOOLS_PER_TURN` portée de 13 à 14. Injection `MEMORY_TOOLS` dans les 7 ToolNode (chat, esg_scoring, carbon, financing, application, credit, action_plan). Refactor `app/graph/checkpointer.py` : `create_checkpointer()` désormais `@asynccontextmanager` propre (entrée/sortie psycopg) qui appelle `AsyncPostgresSaver.setup()` à l'entrée. Lifespan FastAPI dans `app/main.py` ouvre `async with create_checkpointer() as cp:` et compile le graphe avec ce checkpointer (fallback MemorySaver si `OPENROUTER_API_KEY` manquante ou erreur d'init). `_load_context_memory(db, user_id, conversation_id=None)` étendue pour charger les 15 derniers messages bruts de la conversation courante (`ORDER BY created_at DESC LIMIT 15` puis reverse) en plus des 3 résumés (résumés en tête, messages bruts en queue avec format `[<relative_time>, <role>] <content>` borné à 1500 chars). `stream_graph_events` accepte `account_id` paramètre transmis au `RunnableConfig.configurable.account_id` pour permettre au tool `recall_history` de filtrer par tenant. Frontend : composant `ToolCallIndicator.vue` étendu avec libellé « Recherche dans l'historique de conversation… » pour `recall_history` (dark mode complet conservé). 63 nouveaux tests backend (test_service 23 cas masquage/chunking/embedding/search/purge, test_hooks 4 cas listener/dispatch/no-op, test_chat_context_loader 10 cas format_relative_time + 15-messages, test_recall_history_tool 16 cas validation/sérialisation/RLS/cap, test_purge 4 cas cascade/multi-account/user-delete-FR-027, test_checkpointer_persistence 4 cas signature/injection, test_observability 2 cas logs structurés) + 5 tests frontend Vitest ToolCallIndicator + spec Playwright `frontend/tests/e2e/F12-memoire-contextuelle-pgvector.spec.ts` (4 scénarios US1-US4). Couverture 86 % sur les nouveaux modules (app.modules.memory 100 % hooks/init et 85 % service ; app.graph.tools.memory_tools 90 %). 1586 tests backend verts (1523 baseline + 63 F12), 0 régression. 443 frontend tests verts (3 préexistants en échec hors F12). Round-trip Alembic `up/down/up` validé sur PostgreSQL.
- 022-versioning-money-devises (F04): Money typed strict (Pydantic v2 frozen, Currency Literal XOF/EUR/USD/GBP/JPY, amount Decimal(20,2) ge=0, factory `Money.from_columns`) + peg fixe FCFA-EUR `Decimal("655.957")` (`FCFA_EUR_PEG`) + service currency conversion (peg sans HTTP/BDD, table `exchange_rates` avec fallback ascendant, pivot USD systématique pour devises non-peggées avec exception `ConversionPathUnavailableError` explicite). Versioning catalogue : champs `version` (regex `^\d+\.\d+$`), `valid_from`, `valid_to`, `superseded_by` (FK self) sur 12 tables catalogue + `catalog_version` dédié sur sources, mixin SQLAlchemy `VersioningMixin` / `SourceVersioningMixin`, service `bump_version(current, force_major=False)` (1.0 → 1.1 ou 1.0 → 2.0), `supersede(session, model_cls, old_id, new_id)` avec vérification anti-cycle applicative, trigger PL/pgSQL `prevent_supersede_cycle()` PostgreSQL en défense en profondeur (skip SQLite). Snapshot immuable JSONB `snapshot_data` autoportant créé automatiquement à la transition `submitted_to_intermediary` ou `submitted_to_fund` dans `update_application_status` (capture referential/fund/intermediary/scores/source_ids), service `build_snapshot_data` + garde anti-mutation `validate_immutable`, log structuré INFO avec taille bytes pour observabilité, warning si > 100 KB (gzip post-MVP). Endpoint `POST /api/applications/{id}/recompute-against-snapshot` (auth user, vérifie account_id) → recompute depuis snapshot, retourne `{score, comparison_with_origin: {match, delta}, referential_version_used}`, 409 si non soumis. Module `app/modules/currency/` complet (service, exceptions `NoRateAvailableError`/`ConversionPathUnavailableError`/`FetchFailedError`, schemas Pydantic, router public `/api/currency/rates/latest` + `/api/currency/convert`, admin_router `/api/admin/currency/fetch-status`). Script CLI `app/scripts/fetch_exchange_rates.py` exécutable via `python -m app.scripts.fetch_exchange_rates [--force]` avec cap dur 1/jour (vérification `MAX(fetched_at) < 24h`), mode dégradé si `EXCHANGERATE_API_KEY` vide, log structuré ERROR `EXCHANGERATE_FETCH_FAILED` (jamais swallowed), insère USD→{XOF,EUR,GBP,JPY} + dérivation paires inverses 1/rate. Tool LangChain `simulate_financing` corrigé : `_simulate_financing` utilise désormais `fund.min_amount_money` / `fund.max_amount_money` properties (fallback sur `*_xof` legacy si Money typé None) → plus d'AttributeError sur `fund.max_amount`. Frontend : type TypeScript `Money` (amount string + currency Literal), `Currency`, `ExchangeRate`, `ConvertResponse`, composable `useCurrency` (`format`, `convert` via API backend, `getRate`, `getPmeCurrency`=XOF), composants Vue `<MoneyDisplay>` (props money, showPmeCurrency, modeOverride ; lit `displayCurrencyMode` du store ui ; rend natif + équivalent FCFA en mode `both` ; omet équivalent si devise = devise PME ; tooltip `title` ; classes `dark:`) et `<ReferentialBadge>` (libellé « Évalué selon Référentiel <name> v<version> du <date FR> » cliquable → emit `open-source-modal`, dark mode, ARIA aria-label). Store Pinia `ui` étendu : `displayCurrencyMode: 'native' | 'pme' | 'both'` (défaut `both`), persistance `localStorage` clé `mefali.ui.displayCurrencyMode`, validation enum stricte sur le setter. Migration Alembic `022_money_and_versioning` (down_revision='021_audit_log') : CREATE TABLE exchange_rates + 8 lignes seed (USD↔{XOF,EUR,GBP,JPY} + 4 inverses calculées), 12 ALTER TABLE pour colonnes versioning + 1 pour sources (catalog_version), CREATE FUNCTION `prevent_supersede_cycle()` + 13 triggers (PostgreSQL only via `if op.get_bind().dialect.name == 'postgresql'`), 4 ALTER TABLE pour paires Money (funds×2, company_profiles, action_items) avec CHECK currency enum + CHECK pair, ALTER TABLE fund_applications ajoute snapshot_at + snapshot_data JSONB, backfill SQL idempotent `<field>_amount = <field>_xof, <field>_currency = 'XOF'`. `ExchangeRate` ajouté à `EXEMPT_MODELS` (catalogue F03 audit log, lecture publique sans account_id). Cohabitation phase 1 : anciennes colonnes `*_xof` / `*_fcfa` conservées (drop reporté à migration séparée hors-scope F04). 71 nouveaux tests backend (test_core/test_money 10 cas, test_core/test_constants 3 cas, test_models/test_exchange_rate 4 cas, test_versioning/test_supersede 13 cas + test_supersede_integration 2 cas, test_currency/test_service 11 cas + test_router 6 cas + test_admin_router 2 cas + test_fetch_script 4 cas, test_applications/test_snapshot_creation 5 cas + test_snapshot_immutable 4 cas + test_recompute_against_snapshot 4 cas, test_tools/test_simulate_financing_money 3 cas) + 23 tests frontend Vitest (MoneyDisplay 6 cas, ReferentialBadge 5 cas, useCurrency 6 cas, ui.f04 6 cas) + 4 scénarios Playwright `frontend/tests/e2e/F04-versioning-money-devises.spec.ts`. Couverture 92 % sur les nouveaux modules F04 (app.core.money, app.modules.currency, app.modules.versioning, app.modules.applications.snapshot, app.modules.applications.recompute, app.models.exchange_rate). 1523 tests backend verts (1452 baseline + 71 F04), 0 régression. 438 tests frontend verts (3 préexistants en échec hors F04). Round-trip Alembic `up/down/up` validé sur PostgreSQL.
- 021-audit-log (F03): Module audit log append-only complet. Table `audit_log` avec triggers PostgreSQL BEFORE UPDATE/DELETE qui RAISE EXCEPTION (`raise_audit_log_no_update` / `raise_audit_log_no_delete`) + REVOKE UPDATE,DELETE best-effort sur le rôle applicatif (no-op si superuser, NOTICE émis). Mixin SQLAlchemy `Auditable` couplé à un listener global `event.listens_for(Session, 'before_flush')` qui parcourt session.new/dirty/deleted, calcule un diff field-level via `inspect(obj).attrs[field].history`, borne chaque valeur à 10 KB (marqueur `_truncated: true`), et insère N lignes audit_log dans la même transaction (atomicité ACID). Anti-récursion : insertions de AuditLog ignorées. Constantes `AUDITABLE_MODELS` (CompanyProfile, FundApplication, ESGAssessment, CarbonAssessment, CreditScore, ActionPlan, ActionItem) + `EXEMPT_MODELS` documenté (catalogue F01, infra, AuditLog, CarbonEmissionEntry/CreditDataPoint pas d'account_id propre, ESGCriterionScore vit dans assessment_data JSON). ContextVar Python `current_source_of_change` (default 'manual') + helper `source_of_change_scope(value)` (context manager Token-safe). 9 nœuds LangGraph décorés `@_with_llm_source` (chat, esg_scoring, carbon, financing, application, credit, action_plan, document, profiling) → toute mutation via tool LangChain est tracée `source_of_change=llm`. Middleware `AdminAuditContextMiddleware` monté sur `/api/admin/*` → `source_of_change=admin` automatique. Service `AuditService.record_admin_view(admin, target_account_id, request)` idempotent par requête (cache `request.state.audit_view_recorded`), insère ligne `view_admin` côté PME pour transparence. 4 endpoints API : `GET /api/audit/me` (PME, filtres entity_type/entity_id/action/source_of_change/since/until/page/limit/order), `GET /api/audit/me/export` (CSV UTF-8 BOM ou JSON streaming via StreamingResponse), `GET /api/admin/audit/{account_id}` (admin, déclenche view_admin), `GET /api/admin/audit` (admin global). Frontend : page `/historique` (PME, layout default, timeline + filtres + pagination + export), pages `/admin/audit` et `/admin/audit/[accountId]` (layout admin F02, accent rouge), 4 composants Vue (`AuditLogEntry`, `AuditTimeline`, `AuditFilters`, `AuditExportButton`) avec dark mode complet et libellés français (Création/Modification/Suppression/Consultation Admin), composable `useAuditLog.ts` (fetchMe/fetchByAccount/fetchGlobal/exportCsv/exportJson), store Pinia `audit.ts`, types TypeScript `AuditEvent`/`AuditAction`/`AuditSourceOfChange`/`AuditFilters`. Migration Alembic `021_create_audit_log` (down_revision='020_sources') avec ENUMs PG, table, 4 indexes, 2 fonctions PL/pgSQL, 2 triggers, REVOKE best-effort, RLS ENABLE+FORCE + 4 policies. Tests : 76 tests backend (unit context/truncate/csv_writer/mixin/models, intégration endpoints/source_of_change/admin_view/whitelist, postgres triggers/RLS/migration) + 16 tests frontend Vitest (AuditLogEntry/AuditFilters/useAuditLog) + 4 scénarios Playwright `frontend/tests/e2e/F03-audit-log.spec.ts`. Couverture >= 85 % sur le périmètre F03. Documentation `docs/audit-log.md` (modèle de menaces, schéma, requêtes SQL communes, format export, limites MVP RGPD/Merkle/PDF signé/partitionnement/rôle PG séparé, procédure pour rendre auditable un nouveau modèle). Zéro régression sur les 1376 tests baseline.
- 020-fondations-sourcage-catalogue (F01): Catalogue de sources verifiees + sourcage obligatoire de chaque chiffre. 11 nouvelles tables (sources, indicators, criteria, formulas, thresholds, referentials, referential_indicators, emission_factors, required_documents, simulation_factors, unsourced_flags) avec workflow 4-yeux strict (captured_by != verified_by, CHECK constraint applicatif + base) et statuts (draft/pending/verified/outdated). Migration Alembic `020_create_sources_catalog` avec RLS PostgreSQL active (lecture publique des entites verified/published, ecriture admin only via current_setting('app.current_role')). 30+ sources verified seedees automatiquement (ADEME Base Carbone v23, IPCC AR6 WG3, IEA Africa Energy Outlook 2024, Taxonomie verte UEMOA, Circulaire BCEAO 002-2024, GCF Investment Framework, IFC Performance Standards, BOAD Politique Sectorielle ESS, Gold Standard, Verra VCS, ODD ONU 8/9/10/12/13/17, GRI 2021, ISO 14064-1, etc.). Migration des donnees codees en dur (EMISSION_FACTORS, ESGCriterion, SECTOR_WEIGHTS, simulation_factors) vers les tables BDD avec FK source_id obligatoire. 3 tools LangChain `cite_source` / `search_source` / `flag_unsourced` injectes en GLOBAL_WHITELIST des 7 noeuds metiers (chat, esg_scoring, carbon, financing, application, credit, action_plan), borne MAX_TOOLS_PER_TURN portee de 10 a 13. Validator `source_required.py` post-tour LLM detecte les grappes chiffre+unite via regex compilee, ignore les motifs ISO 14001/AR6/COP/ODD/etc., demande retry max 1 puis substitue par fallback texte « [je ne dispose pas d'une source verifiee pour ce chiffre] ». Service `SourceService` avec 7 routes REST `/api/sources` (list/get/create/request-verification/verify/mark-outdated/patch) protegees par `get_current_admin` (sauf list/get qui filtrent verified pour PME). Frontend : 4 composants Vue `SourceLink`, `SourceModal` (Teleport+focus piege), `SourceBadge` (4 statuts colores draft/pending/verified/outdated), `SourcesList` (etat vide/chargement) avec dark mode complet et ARIA roles ; composable `useSources` (cache 5 min) + store Pinia `sources` ; page publique `/sources` filtrable par publisher avec pagination 20/page. Annexe « Sources et references » auto-generee dans le rapport ESG PDF (collecte des cite_source via tool_call_logs, numerotation [n], URL cliquable, libelle « Aucune source mobilisee » si vide). Integrations `<SourceLink>` dans ScoreCard, FinancingCard, esg/Recommendations, esg/StrengthsBadges, esg/CriteriaProgress, credit/Recommendations, pages /carbon/results, /financing/[id], /applications/[id]. Tests : 70 tests backend (modeles, schemas, service, router, tools, validator, seed, migration_helpers) + 24 tests frontend (Vitest sur composants/store) + golden_set_50.json (50 cas annotes pour le validator) + spec E2E Playwright. 1373 tests backend verts (zero regression sur baseline F02). Couverture sources >= 83 %.
- 019-multitenant-roles-rls: Multi-tenant + roles + Row-Level Security PostgreSQL. 3 nouveaux modeles (`Account`, `RefreshToken`, `AccountInvitation`) + extension `User` (role PME/ADMIN, account_id) + colonne `account_id` ajoutee a 14 tables metier (companies, conversations, messages, documents, esg_assessments, carbon_assessments, fund_matches, fund_applications, credit_scores, action_plans, action_items, reminders, interactive_questions, tool_call_logs, reports). Migration Alembic `019_multitenant_and_roles` complete (CREATE TYPE user_role/invitation_status, 3 CREATE TABLE, ALTER TABLE users + 14 tables metier avec backfill via users, ENABLE+FORCE RLS + 2 policies par table). Helper `set_rls_context(session, account_id, role, user_id)` cable dans `get_current_user` (deps.py) — toute route protegee beneficie automatiquement de l'isolation. Refresh token rotatif (rotation systematique + fenetre de grace 5s + revocation `replaced_by_jti`), endpoint `POST /auth/logout` revoque tous les refresh tokens, JWT 24h. Module account complet (`POST /account/invite`, `GET /account/users`, `DELETE /account/users/:id`) avec `LoggingEmailDelivery` (Protocol injection-ready pour SMTP/SendGrid post-MVP). Whitelist email `admin@esg-mefali.com` supprimee de `financing/router.py` au profit de `Depends(get_current_admin)`. Module admin squelette (`GET /admin/health`) protege via dependance commune. Frontend : composant `RoleBadge.vue` (variante ADMIN rouge / PME emerald, dark mode complet), middleware `admin.ts` (route-scope, redirige PME vers /dashboard), layout `admin.vue` (accent rouge, sidebar Catalogue/Sources/Comptes/Metriques pre-cablees pour F09), pages `/admin/health` et `/account/team` (dark mode + modale confirmation retrait), composable `useAccountTeam.ts` (listTeam/inviteMember/removeMember/revokeInvitation), extension store `auth.ts` (champs `account` + `role`, getter `isAdmin`), pages `login.vue`/`register.vue` adaptees pour `?invite=<token>` (banniere d'invitation + relai du token au backend). Tests E2E Playwright `F02-multitenant-roles-rls.spec.ts` (4 scenarios US1-US4) + helpers `F02-helpers.ts` (mock backend complet). Documentation `docs/auth-and-multitenant.md` (modele de menaces, architecture RLS, rotation refresh, ajout table metier, seed Admin, troubleshooting). Script CLI `app.scripts.seed_admin` pour creation Admin off-server. Couverture tests verte : 1294 backend + tests frontend dedies (RoleBadge, middleware admin, useAccountTeam, store auth).
- 018-interactive-chat-widgets: Widgets interactifs pour les questions de l'assistant IA. Nouveau tool LangChain `ask_interactive_question` (4 variantes qcu/qcm/qcu_justification/qcm_justification) injecte dans les 7 noeuds LangGraph (chat, esg_scoring, carbon, financing, application, credit, action_plan). Table satellite `interactive_questions` + migration Alembic `018_create_interactive_questions.py` (aucune modification des tables existantes). Transport via marker `<!--SSE:{"__sse_interactive_question__":true,...}-->` detecte par `stream_graph_events`. 2 nouveaux events SSE (`interactive_question`, `interactive_question_resolved`) + extension `POST /api/chat/messages` (3 champs `interactive_question_*`) + 2 nouveaux endpoints REST (`POST /api/chat/interactive-questions/{id}/abandon`, `GET /api/chat/conversations/{id}/interactive-questions`). Helper `WIDGET_INSTRUCTION` partage injecte dans les 6 prompts modules (esg_scoring, carbon, financing, application, credit, action_plan) + chat_node. Frontend : 5 composants Vue (`InteractiveQuestionHost`, `SingleChoiceWidget`, `MultipleChoiceWidget`, `JustificationField`, `AnswerElsewhereButton`) avec dark mode complet et ARIA roles (`radiogroup`, `checkbox`, `aria-checked`, `aria-describedby`), types TypeScript `InteractiveQuestion*`, extension `useChat.ts` (`currentInteractiveQuestion`, `interactiveQuestionsByMessage`, `submitInteractiveAnswer`, `onInteractiveQuestionAbandoned`), integration dans `ChatMessage.vue` et `pages/chat.vue` avec verrouillage de l'input texte quand une question `pending` existe. Invariant 1 question pending max par conversation (les anciennes sont marquees `expired` avant insertion). Fallback « Repondre autrement » + expiration automatique sur message texte libre (clarification Q4). Justification bornee a 400 caracteres (clarification Q5, defense en profondeur cote serveur). 34 nouveaux tests unitaires + integration, 935 tests backend verts, zero regression.
- 015-fix-toolcall-esg-timeout: Correction 3 anomalies bloquant les tests d'integration (tool calling application/credit + timeout ESG). Nouveau tool create_fund_application dans application_tools.py pour creer des dossiers via le chat. Nouveau tool batch_save_esg_criteria dans esg_tools.py pour sauvegarder N criteres ESG en une seule transaction (evite timeout 30 appels sequentiels). Prompts application.py, credit.py et esg_scoring.py renforces avec ROLE actif, section OUTILS DISPONIBLES et REGLE ABSOLUE forcant le tool calling au lieu de reponses textuelles. Timeout LLM explicite request_timeout=60 dans get_llm(). 14 nouveaux tests unitaires prompts/tools, zero regression sur les 856 tests existants.
- 014-concise-chat-style: Style de communication concis pour l'assistant IA. STYLE_INSTRUCTION injectee dans les 6 modules specialises + conditionnelle dans le chat general (post-onboarding seulement).
- 013-fix-multiturn-routing-timeline: Correction routing multi-tour LangGraph (BUG-1) et format timeline (BUG-2). Mecanisme active_module dans ConversationState (2 champs: active_module str|None, active_module_data dict|None) pour maintenir le contexte entre les tours de conversation. Classification binaire LLM continuation/changement dans router_node avec defaut securitaire (rester dans le module en cas d'erreur). Tous les 9 noeuds specialistes gerent le cycle de vie active_module (activation au demarrage, mise a jour progressive, desactivation a la finalisation). Reprise de module apres interruption via detection in_progress en base. Transition directe entre modules sans perte de donnees. Frontend: normalisation TimelineBlock.vue tolerante aux variantes (phases/items/steps → events, aliases period→date, name→title, state→status, details→description, defaut status=todo). Prompts backend standardises sur format canonique events (action_plan.py, carbon.py, financing.py). 34 tests backend + 21 tests frontend, zero regression.
- 012-langgraph-tool-calling: Integration tool calling LangGraph dans les 9 noeuds du graphe (chat, esg_scoring, carbon, financing, application, credit, action_plan, document, profiling). 32 tools LangChain repartis dans graph/tools/ par module metier (profiling_tools, esg_tools, carbon_tools, financing_tools, application_tools, credit_tools, document_tools, action_plan_tools, chat_tools). Pattern ToolNode conditionnel avec boucle max 5 iterations, retry automatique 1x par tool. SSE refactore avec astream_events() pour streaming natif tokens + tool_call_start/end/error. Journalisation complete des tool calls dans table tool_call_logs. Frontend: composable useChat.ts parse les events tool, composant ToolCallIndicator.vue avec indicateur visuel contextuel en francais. Migration evenements profil (profile_update/profile_completion) depuis l'ancien extract_and_update_profile vers le tool update_company_profile avec metadonnees SSE structurees. Dark mode complet, 100+ tests.
- 011-dashboard-action-plan: Dashboard principal agregeant ESG/carbone/credit/financement en 4 cartes synthetiques + carte financements enrichie avec parcours intermediaires, plan d'action personnalise genere par Claude (10-15 actions multi-categories environment/social/governance/financing/carbon/intermediary_contact avec coordonnees intermediaires snapshot), page plan d'action avec timeline verticale chronologique + filtres par categorie + barre progression globale/par-categorie, systeme de rappels types (action_due/assessment_renewal/fund_deadline/intermediary_followup/custom) avec polling 60s et toasts in-app variante intermediaire bleu, gamification 5 badges automatiques (first_carbon/esg_above_50/first_application/first_intermediary_contact/full_journey), action_plan_node LangGraph avec blocs visuels timeline/table/mermaid/gauge/chart, API REST /api/dashboard + /api/action-plan 8 endpoints, pages /dashboard et /action-plan, dark mode complet, 105 tests
- 008-green-financing-matching: Module conseiller financement vert (BDD 12 fonds reels GCF/FEM/BOAD/BAD/SUNREF/FNDE/etc + 14 intermediaires avec coordonnees + ~50 liaisons fund-intermediary, matching projet-financement par scoring multi-criteres secteur/ESG/taille/localisation/documents, parcours d'acces direct vs intermediaire avec etapes LLM, catalogue fonds filtrable type/secteur/montant/acces/statut, annuaire intermediaires filtrable type/pays, workflow interet→choix intermediaire→fiche preparation PDF WeasyPrint, financing_node LangGraph avec RAG pgvector + blocs visuels mermaid/table/progress/timeline, API REST /api/financing 10+ endpoints, pages /financing liste 3 onglets + /financing/[id] detail fonds, embeddings text-embedding-3-small, dark mode complet, gestion etats vides/erreurs)
- 007-carbon-footprint-calculator: Module calculateur empreinte carbone conversationnel (questionnaire guide par categorie energie/transport/dechets/industriel/agriculture, facteurs emission Afrique Ouest, equivalences parlantes FCFA, carbon_node LangGraph avec visualisations inline chart/gauge/table/timeline, API REST /api/carbon 6 endpoints, page /carbon liste bilans + /carbon/results dashboard donut/barres/equivalences/plan reduction/benchmark sectoriel/evolution temporelle, benchmarks 9 secteurs avec fallback, contrainte unicite bilan/annee, reprise bilans interrompus, dark mode, 57 tests)
- 006-esg-pdf-reports: Module generation rapports ESG PDF (WeasyPrint HTML->PDF, graphiques matplotlib SVG, resume executif IA Claude, template 9 sections, conformite UEMOA/BCEAO/ODD, API REST /api/reports, page /reports liste+preview+download, notification chat SSE, dark mode, edge cases generation simultanee)
- 005-esg-scoring-assessment: Module evaluation et scoring ESG complet (30 criteres E-S-G, ponderation sectorielle, scoring dynamique, esg_scoring_node LangGraph, API REST /api/esg, page resultats /esg, RAG documentaire par critere, benchmark sectoriel avec fallback, historique evaluations Chart.js, reprise evaluations interrompues, 71 tests, 80% couverture)
- 004-document-upload-analysis: Module complet upload/analyse documents (PyMuPDF, pytesseract, OCR, embeddings pgvector, chat integration, dark mode, 81% couverture tests)
- 001-technical-foundation: Added Python 3.12, TypeScript 5.x (strict mode)


## Parallel Sub-agents Strategy

Use multiple sub-agents in parallel for efficiency(10 max):
- Search frontend + backend simultaneously
- Explore multiple files/folders at the same time
